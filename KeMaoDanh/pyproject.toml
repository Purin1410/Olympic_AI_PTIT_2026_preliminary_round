[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ke-mao-danh"
version = "2.0.0"
description = "Real-image training and submission notebooks"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
